FROM golang:1.21-alpine AS builder
WORKDIR /src
COPY . .
RUN go build -o /bin/driftdetector ./cmd/driftdetector

FROM alpine:latest
COPY --from=builder /bin/driftdetector /usr/local/bin/
CMD ["driftdetector"]
